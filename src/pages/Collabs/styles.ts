import styled from 'styled-components';

export const Painel = styled.div`
  border-radius: 12px;
  position: absolute;
  top: 110px;
  padding: 25px;
  background: #fff;

  border: 1px solid rgba(0, 0, 0, 0.2);
`;
